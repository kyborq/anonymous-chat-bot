# Anonka - Анонимный Telegram Бот

Бот для анонимного общения пользователей с администратором. Пользователи могут отправлять анонимные сообщения, а администратор может отвечать на них.

## Возможности

- ✉️ Отправка анонимных текстовых сообщений администратору
- 📸 Отправка анонимных фото и видео (предложки)
- 💬 Ответы администратора на текстовые сообщения пользователей
- 📋 Автоматическое форматирование вопросов для публикации в канал (с цитатами)
- 🗄️ Хранение истории сообщений и медиа в базе данных

## Запуск с помощью Docker

### Предварительные требования

- Docker
- Docker Compose

### Установка и запуск

1. **Клонируйте репозиторий** (если еще не сделали):
   ```bash
   git clone <your-repo>
   cd anonka
   ```

2. **Создайте файл `.env`** с вашими настройками:
   ```bash
   nano .env
   ```
   
   Добавьте следующие переменные:
   ```env
   BOT_TOKEN=your_bot_token_here
   ADMIN_ID=your_admin_id_here
   DB_PATH=sqlite+aiosqlite:////app/data/data.db
   ```
   
   - `BOT_TOKEN` - получите у [@BotFather](https://t.me/BotFather)
   - `ADMIN_ID` - ваш Telegram ID (получите через [@userinfobot](https://t.me/userinfobot))

3. **Создайте директорию для базы данных**:
   ```bash
   mkdir -p data
   ```

4. **Запустите бота**:
   ```bash
   docker-compose up -d
   ```

### Управление ботом

**Просмотр логов:**
```bash
docker-compose logs -f
```

**Остановка бота:**
```bash
docker-compose down
```

**Перезапуск бота:**
```bash
docker-compose restart
```

**Обновление бота:**
```bash
git pull
docker-compose up -d --build
```

## Альтернативный запуск (без Docker)

### Предварительные требования

- Python 3.11+
- pip

### Установка

1. **Создайте виртуальное окружение**:
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate
   ```

2. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

3. **Создайте файл `.env`** (см. выше)

4. **Запустите бота**:
   ```bash
   python src/main.py
   ```

### Запуск как системный сервис

Используйте скрипт `deploy.service.sh`:
```bash
chmod +x deploy.service.sh
./deploy.service.sh
```

## Использование

### Для пользователей

#### Отправка текстового сообщения
1. Запустите бота командой `/start`
2. Нажмите кнопку **"✉️ Отправить сообщение"**
3. Напишите ваше сообщение (оно будет отправлено анонимно)
4. Если администратор ответит, вы получите уведомление

#### Отправка фото или видео (предложки)
1. Нажмите кнопку **"📸 Отправить фото/видео"**
2. Отправьте фото или видео (можно добавить подпись)
3. Ваше медиа будет отправлено администратору анонимно
4. Это предложка - администратор не будет отправлять на неё ответ

### Для администратора

1. Используйте команду `/admin` для просмотра панели администратора
2. Вы будете получать два типа сообщений:
   - **Текстовые сообщения** - требуют ответа
   - **Фото/видео (предложки)** - просто для просмотра

#### Ответ на текстовое сообщение

1. Получите анонимное текстовое сообщение от пользователя
2. Просто ответьте (reply) на это сообщение вашим текстом
3. Бот автоматически:
   - Отправит ваш ответ пользователю
   - Отформатирует сообщение для публикации в канал
   - Пришлет вам готовое сообщение с вопросом в виде цитаты

#### Работа с предложками (фото/видео)

- Предложки отмечены как "📸 Анонимная предложка (фото)" или "🎥 Анонимная предложка (видео)"
- На предложки не нужно отвечать пользователю
- Если попытаетесь ответить на предложку, бот сообщит, что это не поддерживается

**Пример форматированного сообщения:**

```
❓ Анонимный вопрос:

> Как подключиться к вашему сервису?

💬 Ответ:

Для подключения к нашему сервису вам нужно...
```

## Структура проекта

```
anonka/
├── src/
│   ├── handlers/
│   │   ├── admin.py      # Обработчики для администратора
│   │   └── user.py       # Обработчики для пользователей
│   ├── config.py         # Конфигурация
│   ├── database.py       # Работа с базой данных
│   ├── keyboards.py      # Клавиатуры бота
│   ├── models.py         # Модели базы данных
│   └── main.py           # Точка входа
├── data/                 # База данных (создается автоматически)
├── .env                  # Переменные окружения (создайте сами)
├── .dockerignore         # Исключения для Docker
├── Dockerfile            # Инструкции для сборки образа
├── docker-compose.yml    # Конфигурация Docker Compose
├── requirements.txt      # Зависимости Python
└── README.md            # Этот файл

```

## Технологии

- [aiogram](https://docs.aiogram.dev/) - асинхронный фреймворк для Telegram Bot API
- [SQLAlchemy](https://www.sqlalchemy.org/) - ORM для работы с базой данных
- [aiosqlite](https://github.com/omnilib/aiosqlite) - асинхронный драйвер для SQLite
- [python-dotenv](https://github.com/theskumar/python-dotenv) - загрузка переменных окружения

## Лицензия

MIT

